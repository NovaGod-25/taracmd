# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.apk
*.aab
*.keystore
*.jks
# ...except the one that has to be identical everywhere. See the comment in
# android/app/build.gradle.kts: a per-machine debug key is what made every new
# APK refuse to install over the last one.
!android/app/taracmd-debug.p12

# Gradle wrapper jar is generated, not committed — see CLAUDE.md open item 3
android/gradle/wrapper/gradle-wrapper.jar
android/gradlew
android/gradlew.bat

# Answer-key PDFs cached by tools/answer-keys.py. Not redistributed: they are
# fetched from upsc.gov.in and stay local.
tools/.key-pdfs/

# Test deps. tests/package-lock.json IS committed, so CI can npm ci.
tests/node_modules/

# Python
__pycache__/
*.pyc
.venv/

# Editors / OS
.DS_Store
Thumbs.db
*.swp

# Cached upsc.gov.in listing pages (tools/pyq-papers.py). Not ours to commit.
tools/.archive/
